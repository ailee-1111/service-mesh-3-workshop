apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: istiod-monitor
  namespace: USER_NAME_PLACEHOLDER-istio-system
spec:
  endpoints:
  - interval: 30s
    port: http-monitoring
  selector:
    matchLabels:
      istio: pilot
  targetLabels:
  - app
